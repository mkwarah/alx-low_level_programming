#include "main.h"
/**
 * print_alphabet - printing the alphabet in lower cases
 * Return: Nothing to return in this code
 */
void print_alphabet(void)
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
	{
		_putchar(x);
	}
	_putchar('\n');
}
